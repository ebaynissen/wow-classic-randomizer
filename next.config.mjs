/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/wow-classic-generator",
    output: "export",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
