/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/wow-classic-randomizer",
    output: "export",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
